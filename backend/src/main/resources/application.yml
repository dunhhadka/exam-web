server:
  port: 8082

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/exam?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:123456}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

  cache:
    type: redis
    redis:
      time-to-live: PT60M
      cache-null-values: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: vichatvn@gmail.com
    password: vbog qofy wone ckul
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true
custom:
  cache:
    custom-cache:
      users:
        time-to-live: PT30M
        cache-null-values: false
      sessions:
        time-to-live: PT120M
        cache-null-values: false
      exams:
        time-to-live: PT60M
        cache-null-values: false

security:
  authentication:
    jwt:
      secret-key: ${SECRET_KEY:4B56EB985E5AC77C2D5457AB78D7D2B6D7ECA569D92560FA716E73E57CD9E1746F67D8CEA9663F1022A4DAE1744125D9EC36FF436F5A911420BB576B4817DA43}
      access-token-expires-in: ${ACCESS_TOKEN_EXPIRES_IN:60000m}
      refresh-token-expires-in: ${REFRESH_TOKEN_EXPIRES_IN:1d}
      refresh-token-long-expires-in: ${REFRESH_TOKEN_LONG_EXPIRES_IN:30d}

