server:
  port: 1111

spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/exam?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}

  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC
        dialect: org.hibernate.dialect.MySQL8Dialect

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:123456}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms

  cache:
    type: redis
    redis:
      time-to-live: PT60M
      cache-null-values: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: vichatvn@gmail.com
    password: vbog qofy wone ckul
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          auth: true

custom:
  cache:
    custom-cache:
      users:
        time-to-live: PT30M
        cache-null-values: false
      sessions:
        time-to-live: PT120M
        cache-null-values: false
      exams:
        time-to-live: PT60M
        cache-null-values: false

security:
  authentication:
    jwt:
      secret-key: ${SECRET_KEY:4B56EB985E5AC77C2D5457AB78D7D2B6D7ECA569D92560FA716E73E57CD9E1746F67D8CEA9663F1022A4DAE1744125D9EC36FF436F5A911420BB576B4817DA43}
      access-token-expires-in: ${ACCESS_TOKEN_EXPIRES_IN:60000m}
      refresh-token-expires-in: ${REFRESH_TOKEN_EXPIRES_IN:1d}
      refresh-token-long-expires-in: ${REFRESH_TOKEN_LONG_EXPIRES_IN:30d}

# AWS S3 Configuration
aws:
  s3:
    access-key: ${AWS_S3_ACCESS_KEY:minioadmin}
    secret-key: ${AWS_S3_SECRET_KEY:minioadmin}
    region: ${AWS_S3_REGION:us-east-1}
    bucket-name: ${AWS_S3_BUCKET_NAME:exam-bucket}
    presigned-url-expiration-minutes: ${AWS_S3_PRESIGNED_URL_EXPIRATION_MINUTES:1440}
    base-url: ${AWS_S3_BASE_URL:http://localhost:9000}